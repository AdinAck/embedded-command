[package]
name = "dispatch-bundle"
version = "0.1.0"
edition = "2021"

[features]
packit = []

[dependencies]
macros = { package = "embedded-command-macros", path = "../macros" }

[dev-dependencies]
serac = { path = "../serac" }
