[package]
name = "dispatch-bundle"
version = "0.1.0"
edition = "2021"

[features]
packit = []

[dependencies]
macros = { package = "embedded-command-macros", version = "0.4.0" }

[dev-dependencies]
serac = { path = "../serac" }
